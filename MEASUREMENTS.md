# 측정값

## 촬영
| | |
|---|---|
| 영상 | `take1_놀이대_한바퀴_91s.mp4` · 1920x1080 · 91.4초 · 43.1 MB |
| 영상 sha256 | `d46966a649bf7d8e` |
| 뽑은 장 | 24장 (간격 3.8초) · 긴 변 640px |
| 선명도(라플라시안 분산) | 중앙값 1312 · 최소 850 · 최대 1591 |
| 유독 번진 장 | 0개  |

## 판정 — 카메라가 퍼진 정도
| | |
|---|---|
| 카메라가 퍼진 범위 | 27.426 |
| 장면 대각선 | 53.979 |
| **카메라가 퍼진 정도** | **0.508** (기준선 0.1) |
| 판정 | ✅ 통과 — 걸어 다니며 찍은 것 |

## 점
| | |
|---|---|
| 모은 점 | 2,899,526 |
| 버린 점 (각 축 위아래 0.5%) | 84,492 (2.9%) |
| **남은 점 = 알갱이 개수** | **2,815,034** |
| 알갱이 반지름 중앙값 | 0.0268 |

## 형식별로 잰 값
| 형식 | 파일 크기 | 알갱이 개수 | 알갱이당 바이트 |
|---|---|---|---|
| `ply` | 182.6 MB | 2,815,034 | 68.0 |
| `compressed.ply` | 43.7 MB | 2,815,034 | 16.3 |
| `sog` | 14.8 MB | 2,815,034 | 5.5 |
| `spz` | 22.9 MB | 2,815,034 | 8.5 |

- 네 파일의 알갱이 개수가 서로 같다 ✅ — 형식만 바꾼 것
- `.ply` 의 알갱이당 68바이트는 **각도별 색(`f_rest`)이 비어 있다는 뜻**이고, 재서 채운 파일은 그게 정상이다

## 환경
| | |
|---|---|
| GPU | Tesla T4 |
| torch | 2.11.0+cu128 (CUDA 12.8) |
| bf16 지원 | True → 정밀도 **fp16** 로 돌림 |
| 모델 | `facebook/map-anything-apache` (Apache-2.0) |
| 복원 시간 | 11초 |
| 최대 GPU 메모리 | 13.5 GiB |

---

## 웹에 올린 장면 — 배경을 잘라 낸 뒤 (로컬에서 추가로 한 일)

전체 장면을 뷰어에 띄우니 배경의 거대한 알갱이가 화면을 덮어 대상이 안 보였다.
`points.npz` 를 로컬에서 다시 읽어 셋을 적용했다.

| 한 일 | 값 |
|---|---|
| 카메라 고리 안쪽만 남김 | 고리 중심 `[-0.48, -0.04, 8.08]` · 최대반지름 11.55 → **0.6배(6.93) 안쪽** |
| 눈높이보다 위를 잘라 냄 | `Y >= -4.0` (OpenCV 규약이라 음수가 위쪽) |
| 알갱이 반지름 상한 | 85백분위 = **0.0327** |

| | 전체 | 웹 |
|---|---|---|
| 알갱이 | 2,815,034 | **2,264,453** (80.4%) |
| 장면 대각선 | 54.0 | **19.8** |
| `.sog` | 14.8 MB | **12.6 MiB** |

| 형식 | 파일 크기 | 알갱이 개수 | 알갱이당 바이트 |
|---|---|---|---|
| `ply` | 146.8 MB | 2,264,453 | 68.0 |
| `compressed.ply` | 35.2 MB | 2,264,453 | 16.3 |
| `sog` | 12.6 MiB | 2,264,453 | 5.9 |
| `spz` | 17.7 MB | 2,264,453 | 8.2 |

- 네 파일의 알갱이 개수가 서로 같다 ✅
- 점의 분포로 자른 반지름을 정했다 — 25% 3.08 / 50% 4.52 / **75% 6.14** / 90% 13.30 / 95% 18.00
  (고리반지름의 0.53배 안에 4분의 3이 들어 있어서 0.6배로 잘랐다)
